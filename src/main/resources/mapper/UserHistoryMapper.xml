<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.am.adastra.mapper.UserHistoryMapper">

    <select id="getAll" resultType="com.am.adastra.entity.dto.VideoOperateDTO">
        select id, user_id, pid, aid
        from history
        where user_id = #{user_id}
          and state = 'normal'
    </select>

    <insert id="add">
        insert into history(user_id, pid, aid)
        values (#{user_id}, #{pid}, #{aid})
    </insert>
</mapper>
